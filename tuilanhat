-- Garden Bee Flyer v2.3 - FIXED UI VISIBILITY
-- Museum Digpile + Ghost + Eggs + Spring + Spins
-- UI GUARANTEED TO SHOW
-- Inject via executor

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local rootPart = character:WaitForChild("HumanoidRootPart")

-- Wait for PlayerGui explicitly
local playerGui
while not playerGui do
    playerGui = player:WaitForChild("PlayerGui")
    task.wait(0.1)
end

local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

-- CONFIG
local CONFIG = { FlySpeed = 32, HoverHeight = 1, ItemCooldown = 1.5 }
local MIN_Y = 0
local GHOST_MIN_Y = -5
local SWATTER_TYPE = "Swatter"

-- ENABLED FEATURES
local ENABLED = {
    eggs = true,
    ghost = true,
    springEvent = true,
    playtimeRewards = false,
    dailySpin = false,
    museumDig = true,
}

-- STATE VARIABLES
local springEventExecutionCount = 0
local springEventLastExecution = 0
local SPRING_EVENT_COOLDOWN = 1200
local playtimeRewardIndex = 1
local playtimeRewardCollected = 0
local dailySpinCount = 0
local dailySpinLoopActive = false

-- CANDY REFERENCE
local candyRef = ReplicatedStorage:WaitForChild("Storage"):WaitForChild("Materials"):WaitForChild("Candy")
local function getCandyCount()
    local val = candyRef:FindFirstChildWhichIsA("IntValue") or candyRef:FindFirstChildWhichIsA("NumberValue")
    return val and val.Value or 0
end

-- HELPERS
local function isHoneypot(obj)
    local n = obj.Name
    return n:find("Honeypot") ~= nil or n:find("_HoneyPot") ~= nil
end

local function getBasePart(obj)
    if obj:IsA("BasePart") then return obj end
    if obj:IsA("Model") then return obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart", true) end
    return nil
end

local function getPrompt(obj)
    local prompt = obj:FindFirstChildWhichIsA("ProximityPrompt", true)
    if not prompt and obj.Parent then prompt = obj.Parent:FindFirstChildWhichIsA("ProximityPrompt", true) end
    return prompt
end

local function firePrompt(prompt, holdDuration)
    local ok = pcall(fireproximityprompt, prompt, holdDuration or 0)
    if not ok then
        pcall(function()
            prompt:InputHoldBegin()
            task.wait(holdDuration or 0)
            prompt:InputHoldEnd()
        end)
    end
end

local function teleportTo(pos)
    local noclipConn = RunService.Stepped:Connect(function()
        for _, p in ipairs(character:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide = false end
        end
    end)
    rootPart.CFrame = CFrame.new(pos)
    task.wait(0.05)
    noclipConn:Disconnect()
end

local function getActiveEvent()
    for _, obj in ipairs(workspace:GetChildren()) do
        if obj:IsA("Model") and string.match(obj.Name, "^FastGhost_%d+$") then return "ghost" end
    end
    return nil
end

-- REMOTES
local evRemotes = ReplicatedStorage:WaitForChild("Events")
local equipRemote = evRemotes:WaitForChild("EquipItem")
local swatterRemote = evRemotes:WaitForChild("Swatter")
local digRemote = evRemotes:WaitForChild("Dig")

-- FLY SYSTEM
local flying = false
local bv, bg

local function stopFly()
    flying = false
    if bv then bv:Destroy(); bv = nil end
    if bg then bg:Destroy(); bg = nil end
    pcall(function()
        humanoid.PlatformStand = false
        humanoid.AutoRotate = true
    end)
end

local function startFly()
    stopFly()
    flying = true
    humanoid.PlatformStand = true
    humanoid.AutoRotate = false
    
    local noclipConn = RunService.Stepped:Connect(function()
        for _, p in ipairs(character:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide = false end
        end
    end)

    bv = Instance.new("BodyVelocity")
    bv.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    bv.Velocity = Vector3.zero
    bv.Parent = rootPart

    bg = Instance.new("BodyGyro")
    bg.MaxTorque = Vector3.new(1e5, 1e5, 1e5)
    bg.P = 1e4
    bg.CFrame = rootPart.CFrame
    bg.Parent = rootPart
end

UserInputService.InputBegan:Connect(function(inp, gp)
    if gp then return end
    if inp.KeyCode == Enum.KeyCode.F then
        if flying then stopFly() else startFly() end
    end
end)

RunService.Heartbeat:Connect(function()
    if not flying or not bv or not bg then return end
    local cam = workspace.CurrentCamera
    local dir = Vector3.zero
    if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir += cam.CFrame.LookVector end
    if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir -= cam.CFrame.LookVector end
    if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir -= cam.CFrame.RightVector end
    if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir += cam.CFrame.RightVector end
    if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir += Vector3.new(0, 1, 0) end
    if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then dir -= Vector3.new(0, 1, 0) end
    if dir.Magnitude > 0 then
        bv.Velocity = dir.Unit * CONFIG.FlySpeed
        bg.CFrame = CFrame.new(Vector3.zero, dir)
    else
        bv.Velocity = Vector3.zero
    end
end)

-- MUSEUM DIG
local diggingActive = false

local function findDigPiles()
    local piles = {}
    local events = workspace:FindFirstChild("Events")
    if not events then return piles end
    for _, child in ipairs(events:GetChildren()) do
        if child.Name:match("^DigPile_%d+$") then
            local primary = child:FindFirstChild("PrimaryPart") or child:FindFirstChildWhichIsA("BasePart")
            if primary then
                local prompt = primary:FindFirstChildWhichIsA("ProximityPrompt")
                if prompt then
                    table.insert(piles, { model = child, part = primary, prompt = prompt, index = child.Name })
                end
            end
        end
    end
    return piles
end

local function digPile(pile)
    if diggingActive then return end
    diggingActive = true
    if not flying then startFly() end
    teleportTo(pile.part.Position + Vector3.new(0, 2, 0))
    task.wait(0.1)
    firePrompt(pile.prompt, 0)
    pcall(function() digRemote:FireServer("PlayDigVFX", pile.index) end)
    pcall(function() digRemote:FireServer("CompleteDig", pile.index) end)
    diggingActive = false
end

task.spawn(function()
    while true do
        if ENABLED.museumDig then
            local piles = findDigPiles()
            if #piles > 0 then
                for _, pile in ipairs(piles) do
                    if not ENABLED.museumDig then break end
                    digPile(pile)
                    task.wait(0)
                end
            else
                task.wait(1)
            end
        else
            task.wait(1)
        end
    end
end)

-- GHOST SYSTEM
local ghostLoopRunning = false
local ghostHeartbeat = nil
local ghostFlySpeed = 70
local chaseDistance = 3.2
local dodgeDistance = 5
local hitCooldown = 0.05
local ghostTarget = nil
local ghostHitting = false
local ghostHitStart = 0
local lastHitTime = 0
local lastReequipTime = 0
local candyChecking = false
local ghostRayParams = RaycastParams.new()
ghostRayParams.FilterType = Enum.RaycastFilterType.Exclude

local function equipSwatter()
    local char = player.Character
    local hum = char and char:FindFirstChild("Humanoid")
    if hum and player:FindFirstChild("Backpack") then
        local tool = player.Backpack:FindFirstChild(SWATTER_TYPE)
        if tool then hum:EquipTool(tool); return end
    end
    pcall(function() equipRemote:InvokeServer(SWATTER_TYPE, 0) end)
    pcall(function() equipRemote:FireServer(SWATTER_TYPE, 0) end)
end

local function isSwatterEquipped()
    local char = player.Character
    if not char then return false end
    local tool = char:FindFirstChildWhichIsA("Tool")
    return tool ~= nil and tool.Name == SWATTER_TYPE
end

local function findGhost()
    local best, bestDist = nil, math.huge
    for _, obj in pairs(workspace:GetChildren()) do
        if obj:IsA("Model") and string.match(obj.Name, "^FastGhost_%d+$") then
            local pivot = obj:GetPivot()
            if pivot then
                if pivot.Position.Y < GHOST_MIN_Y then continue end
                local dist = (pivot.Position - rootPart.Position).Magnitude
                if dist < bestDist then bestDist = dist; best = obj end
            end
        end
    end
    return best
end

local function startGhostLoop()
    if ghostLoopRunning then return end
    ghostTarget = nil
    ghostHitting = false
    candyChecking = false
    lastReequipTime = 0
    stopFly()
    equipSwatter()
    task.wait(1)
    ghostLoopRunning = true
    ghostRayParams.FilterDescendantsInstances = {character}

    ghostHeartbeat = RunService.Heartbeat:Connect(function()
        if not ghostLoopRunning or not ENABLED.ghost then ghostHitting = false; return end
        local char = player.Character
        if not char then return end
        local root = char:FindFirstChild("HumanoidRootPart")
        if not root then return end

        if not ghostTarget or not ghostTarget.Parent then
            ghostTarget = findGhost()
            ghostHitting = false
            if not ghostTarget then root.Velocity = Vector3.zero; return end
            equipSwatter()
        end

        local pivot = ghostTarget:GetPivot()
        if pivot and pivot.Position.Y < GHOST_MIN_Y then
            ghostTarget = nil
            ghostHitting = false
            root.Velocity = Vector3.zero
            return
        end

        local currentPos = root.Position
        local targetPos = ghostTarget:GetPivot().Position
        local direction = (targetPos - currentPos)
        local distance = direction.Magnitude
        if distance > 0.1 then direction = direction.Unit else direction = Vector3.zero end
        local moveDir = direction * ghostFlySpeed

        if distance > 0.1 then
            local result = workspace:Raycast(currentPos, direction * dodgeDistance, ghostRayParams)
            if result then
                local right = direction:Cross(Vector3.new(0, 1, 0)).Unit
                local left = -right
                if not workspace:Raycast(currentPos, (direction + left * 0.7).Unit * dodgeDistance, ghostRayParams) then
                    moveDir = (direction + left * 0.85).Unit * ghostFlySpeed
                elseif not workspace:Raycast(currentPos, (direction + right * 0.7).Unit * dodgeDistance, ghostRayParams) then
                    moveDir = (direction + right * 0.85).Unit * ghostFlySpeed
                else
                    moveDir = (direction + Vector3.new(0, 0.8, 0)).Unit * ghostFlySpeed
                end
            end
        end

        if distance < 8 then
            local heightDiff = currentPos.Y - targetPos.Y
            if heightDiff > 4 then moveDir = moveDir * 0.6 + Vector3.new(0, -35, 0) end
        end
        root.Velocity = moveDir

        if distance <= chaseDistance then
            if not ghostHitting then
                ghostHitting = true
                ghostHitStart = tick()
                equipSwatter()
            end
            local now = tick()
            if now - lastReequipTime >= 2 then
                lastReequipTime = now
                if not isSwatterEquipped() then equipSwatter() end
            end
            if (now - ghostHitStart) < 120 then
                if (now - lastHitTime) >= hitCooldown then
                    lastHitTime = now
                    if not candyChecking then
                        candyChecking = true
                        local candyBefore = getCandyCount()
                        pcall(function() swatterRemote:FireServer() end)
                        task.spawn(function()
                            task.wait(0.5)
                            local candyAfter = getCandyCount()
                            if candyAfter > candyBefore then
                                ghostHitting = false
                                ghostTarget = nil
                                task.wait(1.5)
                            end
                            candyChecking = false
                        end)
                    else
                        pcall(function() swatterRemote:FireServer() end)
                    end
                end
            else
                ghostHitting = false
                ghostTarget = nil
            end
        else
            if ghostHitting then ghostHitting = false end
        end

        if root.Position.Y - targetPos.Y > 6 and distance < 5 then
            root.CFrame = root.CFrame * CFrame.new(0, -1.5, 0)
        end
    end)
end

local function stopGhostLoop()
    ghostLoopRunning = false
    ghostHitting = false
    ghostTarget = nil
    candyChecking = false
    if ghostHeartbeat then ghostHeartbeat:Disconnect(); ghostHeartbeat = nil end
end

task.spawn(function()
    while true do
        if ENABLED.ghost and getActiveEvent() == "ghost" then
            if not ghostLoopRunning then startGhostLoop() end
        else
            if ghostLoopRunning then stopGhostLoop() end
        end
        task.wait(1)
    end
end)

-- EGGS
local collecting = false
local function collectItem(part, prompt, label, holdDuration)
    if collecting then return false end
    if not part or not part.Parent then return false end
    collecting = true
    teleportTo(part.Position)
    task.wait(0.1)
    if prompt then
        firePrompt(prompt, holdDuration or 0)
        task.wait((holdDuration or 0) + 0.2)
    end
    task.wait(CONFIG.ItemCooldown)
    collecting = false
    return true
end

local function runEggs()
    for _, obj in ipairs(workspace:GetChildren()) do
        if obj.Parent == workspace and obj.Name:find("Egg") and not isHoneypot(obj) then
            local part = getBasePart(obj)
            if part and part.Position.Y >= MIN_Y then
                local prompt = getPrompt(obj)
                if prompt then
                    collectItem(part, prompt, "Egg: " .. obj.Name, 0.25)
                    if not ENABLED.eggs then break end
                end
            end
        end
    end
end

task.spawn(function()
    while true do
        if ENABLED.eggs and not collecting then task.spawn(runEggs) end
        task.wait(2)
    end
end)

-- SPRING EVENT
local function claimSpringEvent()
    if not ENABLED.springEvent then return end
    local now = tick()
    if now - springEventLastExecution < SPRING_EVENT_COOLDOWN then return end
    pcall(function()
        local args = {{ Action = "DeliverQuest", QuestIndex = 1 }}
        ReplicatedStorage:FindFirstChild("Events"):FindFirstChild("SpecialEvent"):FireServer(unpack(args))
        springEventExecutionCount += 1
        springEventLastExecution = now
    end)
end

task.spawn(function()
    while true do
        task.wait(1)
        claimSpringEvent()
    end
end)

-- PLAYTIME REWARDS
local function claimPlaytimeReward()
    if not ENABLED.playtimeRewards then return end
    for index = 1, 9 do
        pcall(function()
            local args = {{ Action = "ClaimReward", Index = index }}
            ReplicatedStorage:FindFirstChild("Events"):FindFirstChild("PlaytimeRewardsHandler"):FireServer(unpack(args))
            playtimeRewardCollected += 1
            playtimeRewardIndex = index
        end)
        task.wait(0.3)
    end
end

task.spawn(function()
    while true do
        task.wait(5)
        claimPlaytimeReward()
    end
end)

-- DAILY SPIN
local function runDailySpin()
    if not ENABLED.dailySpin or not dailySpinLoopActive then return end
    pcall(function()
        local args = { "spin", "daily" }
        ReplicatedStorage:FindFirstChild("Events"):FindFirstChild("DailySpin"):FireServer(unpack(args))
        dailySpinCount += 1
    end)
end

task.spawn(function()
    while true do
        task.wait(1)
        if ENABLED.dailySpin and not dailySpinLoopActive then
            dailySpinLoopActive = true
        elseif not ENABLED.dailySpin and dailySpinLoopActive then
            dailySpinLoopActive = false
        end
    end
end)

task.spawn(function()
    while true do
        task.wait(0.8)
        if dailySpinLoopActive then runDailySpin() end
    end
end)

-- ============================================================
-- UI SECTION - COMPLETELY REBUILT FOR RELIABILITY
-- ============================================================

-- Clear any existing UI
for _, g in ipairs(playerGui:GetChildren()) do
    if g.Name == "GardenBeeFlyer" then g:Destroy() end
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "GardenBeeFlyer"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

-- Colors
local COL = {
    bg = Color3.fromRGB(18, 18, 22),
    panel = Color3.fromRGB(28, 28, 34),
    border = Color3.fromRGB(50, 50, 60),
    text = Color3.fromRGB(220, 220, 230),
    muted = Color3.fromRGB(120, 120, 140),
    green = Color3.fromRGB(34, 197, 94),
    gray = Color3.fromRGB(70, 70, 85),
}

-- Main container with fixed height
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 240, 0, 450)
mainFrame.Position = UDim2.new(0, 15, 0.5, -200)
mainFrame.BackgroundColor3 = COL.bg
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 10)
local stroke = Instance.new("UIStroke")
stroke.Color = COL.border
stroke.Thickness = 1
stroke.Parent = mainFrame

-- Scroll frame for content
local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Name = "ScrollFrame"
scrollFrame.Size = UDim2.new(1, 0, 1, -40)
scrollFrame.Position = UDim2.new(0, 0, 0, 40)
scrollFrame.BackgroundTransparency = 1
scrollFrame.BorderSizePixel = 0
scrollFrame.ScrollBarThickness = 0
scrollFrame.Parent = mainFrame

local listLayout = Instance.new("UIListLayout")
listLayout.SortOrder = Enum.SortOrder.LayoutOrder
listLayout.Padding = UDim.new(0, 0)
listLayout.Parent = scrollFrame

-- Title bar
local titleBar = Instance.new("Frame")
titleBar.Name = "TitleBar"
titleBar.Size = UDim2.new(1, 0, 0, 40)
titleBar.BackgroundColor3 = COL.panel
titleBar.BorderSizePixel = 0
titleBar.LayoutOrder = 0
titleBar.Parent = mainFrame

Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0, 10)

local titleText = Instance.new("TextLabel")
titleText.Text = "Garden Bee Flyer"
titleText.TextSize = 14
titleText.Font = Enum.Font.GothamBold
titleText.TextColor3 = COL.text
titleText.BackgroundTransparency = 1
titleText.Position = UDim2.new(0, 15, 0, 0)
titleText.Size = UDim2.new(0.7, 0, 1, 0)
titleText.TextXAlignment = Enum.TextXAlignment.Left
titleText.Parent = titleBar

local minimizeBtn = Instance.new("TextButton")
minimizeBtn.Text = "−"
minimizeBtn.TextSize = 16
minimizeBtn.Font = Enum.Font.GothamBold
minimizeBtn.TextColor3 = COL.muted
minimizeBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
minimizeBtn.BorderSizePixel = 0
minimizeBtn.Size = UDim2.new(0, 30, 0, 30)
minimizeBtn.Position = UDim2.new(1, -35, 0.5, -15)
minimizeBtn.Parent = titleBar
Instance.new("UICorner", minimizeBtn).CornerRadius = UDim.new(0, 5)

-- Helper functions
local function makeSection(title, layoutOrder)
    local container = Instance.new("Frame")
    container.Name = title .. "Section"
    container.Size = UDim2.new(1, 0, 0, 0)
    container.AutomaticSize = Enum.AutomaticSize.Y
    container.BackgroundTransparency = 1
    container.LayoutOrder = layoutOrder
    container.Parent = scrollFrame

    local padding = Instance.new("UIPadding")
    padding.PaddingLeft = UDim.new(0, 10)
    padding.PaddingRight = UDim.new(0, 10)
    padding.PaddingTop = UDim.new(0, 8)
    padding.PaddingBottom = UDim.new(0, 8)
    padding.Parent = container

    local layout = Instance.new("UIListLayout")
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Padding = UDim.new(0, 6)
    layout.Parent = container

    -- Section title
    local sectionTitle = Instance.new("TextLabel")
    sectionTitle.Text = title
    sectionTitle.TextSize = 10
    sectionTitle.Font = Enum.Font.GothamBold
    sectionTitle.TextColor3 = COL.muted
    sectionTitle.BackgroundTransparency = 1
    sectionTitle.TextXAlignment = Enum.TextXAlignment.Left
    sectionTitle.Size = UDim2.new(1, 0, 0, 14)
    sectionTitle.LayoutOrder = 0
    sectionTitle.Parent = container

    return container
end

local function makeToggle(parent, label, key, layoutOrder)
    local toggle = Instance.new("Frame")
    toggle.Size = UDim2.new(1, 0, 0, 32)
    toggle.BackgroundColor3 = COL.panel
    toggle.BorderSizePixel = 0
    toggle.LayoutOrder = layoutOrder
    toggle.Parent = parent

    Instance.new("UICorner", toggle).CornerRadius = UDim.new(0, 6)

    local label_text = Instance.new("TextLabel")
    label_text.Text = label
    label_text.TextSize = 12
    label_text.Font = Enum.Font.GothamMedium
    label_text.TextColor3 = COL.text
    label_text.BackgroundTransparency = 1
    label_text.TextXAlignment = Enum.TextXAlignment.Left
    label_text.Position = UDim2.new(0, 10, 0, 0)
    label_text.Size = UDim2.new(0.6, 0, 1, 0)
    label_text.Parent = toggle

    local toggleBg = Instance.new("Frame")
    toggleBg.Size = UDim2.new(0, 38, 0, 20)
    toggleBg.Position = UDim2.new(1, -48, 0.5, -10)
    toggleBg.BackgroundColor3 = ENABLED[key] and COL.green or COL.gray
    toggleBg.BorderSizePixel = 0
    toggleBg.Parent = toggle

    Instance.new("UICorner", toggleBg).CornerRadius = UDim.new(1, 0)

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 16, 0, 16)
    knob.Position = ENABLED[key] and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.BorderSizePixel = 0
    knob.Parent = toggleBg

    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local button = Instance.new("TextButton")
    button.Size = UDim2.new(1, 0, 1, 0)
    button.BackgroundTransparency = 1
    button.Text = ""
    button.Parent = toggle

    button.MouseButton1Click:Connect(function()
        ENABLED[key] = not ENABLED[key]
        local isOn = ENABLED[key]
        TweenService:Create(toggleBg, TweenInfo.new(0.2), { BackgroundColor3 = isOn and COL.green or COL.gray }):Play()
        TweenService:Create(knob, TweenInfo.new(0.2), { Position = isOn and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8) }):Play()
    end)
end

-- Build UI sections
local eventSection = makeSection("EVENTS", 1)
makeToggle(eventSection, "Museum Digpile", "museumDig", 1)
makeToggle(eventSection, "Ghost", "ghost", 2)
makeToggle(eventSection, "Eggs", "eggs", 3)
makeToggle(eventSection, "Spring Event", "springEvent", 4)

local spinSection = makeSection("SPINS", 2)
makeToggle(spinSection, "Daily Spin", "dailySpin", 1)
makeToggle(spinSection, "Playtime Rewards", "playtimeRewards", 2)

-- Minimize functionality
local isMinimized = false
minimizeBtn.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    minimizeBtn.Text = isMinimized and "+" or "−"
    scrollFrame.Visible = not isMinimized
    if isMinimized then
        mainFrame.Size = UDim2.new(0, 240, 0, 40)
    else
        mainFrame.Size = UDim2.new(0, 240, 0, 450)
    end
end)

-- Drag functionality
local dragging = false
local dragStart = nil
local dragPos = nil

titleBar.InputBegan:Connect(function(inp)
    if inp.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = inp.Position
        dragPos = mainFrame.Position
    end
end)

UserInputService.InputChanged:Connect(function(inp)
    if dragging and inp.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = inp.Position - dragStart
        mainFrame.Position = UDim2.new(dragPos.X.Scale, dragPos.X.Offset + delta.X, dragPos.Y.Scale, dragPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(inp)
    if inp.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
end)

print("[Garden Bee Flyer v2.3] ✓ Loaded successfully!")
print("[Garden Bee Flyer v2.3] ✓ UI should be visible in top-left corner")
print("[Garden Bee Flyer v2.3] ✓ Museum Digpile and Ghost toggles active")
